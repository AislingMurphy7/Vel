<?xml version="1.0" encoding="utf-8"?>
<!--
    This menu XML file is used for the dropdown
    toolbar menu that provides the user with additional
    options, such as 'Settings', 'Language change', 'Help'
    and 'Exit'.
-->
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="@string/settings"
        android:id="@+id/action_settings"
        app:showAsAction="never" >
    </item>

    <item android:title="@string/lang"
        android:id="@+id/action_Language"
        app:showAsAction="never" >
    </item>

    <item android:title="@string/help"
        android:id="@+id/action_help"
        app:showAsAction="never" >
    </item>

    <item android:title="@string/exit"
        android:id="@+id/action_exit"
        app:showAsAction="never" >
    </item>

</menu>