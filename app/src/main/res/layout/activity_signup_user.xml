<?xml version="1.0" encoding="utf-8"?>

<!--
    This XML file designs the layout for the 'SignupUser.class'
    This XML file defines an 'ImageView' to display the VéL app logo,
    two 'EditTexts' which take input in the form of Email and Password,
    a 'ProgressBar' which shows information is being processed, a 'Button'
    which allows the user to sign up once all information entered is correct
    and finally a clickable 'TextView' which redirects the user to the login
    page if needed.
-->

<android.support.v7.widget.LinearLayoutCompat
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/gradient_background"
    tools:context=".SignupUser">

    <ImageView
        android:id="@+id/vel"
        android:paddingTop="40sp"
        android:contentDescription="@string/imagedesc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/vel"
        android:layout_gravity="center">
    </ImageView>

    <EditText
        android:id="@+id/editTextEmail"
        android:paddingTop="30sp"
        android:layout_height="wrap_content"
        android:layout_width="300dp"
        android:layout_gravity="center_horizontal"
        android:hint="@string/prompt_email"
        android:inputType="textEmailAddress"
        android:paddingBottom="15dp">
    </EditText>

    <EditText
        android:id="@+id/editTextPassword"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:hint="@string/prompt_password"
        android:inputType="textPassword">
    </EditText>

    <ProgressBar
        android:id="@+id/progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:visibility="gone">
    </ProgressBar>

    <Button
        android:id="@+id/buttonSignup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_margin="7dp"
        android:text="@string/Signup"
        android:textColor="@color/black">
    </Button>

    <TextView
        android:id="@+id/textViewLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/have_Acc"
        android:textSize="20sp"
        android:textColor="@color/black">
    </TextView>

</android.support.v7.widget.LinearLayoutCompat>


